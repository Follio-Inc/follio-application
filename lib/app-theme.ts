/** Follio application UI theme (next-themes) — independent of resume color theme. */
export type AppThemeMode = 'light' | 'dark' | 'system';

/** Default theme for the Follio application. */
export const APP_THEME_DEFAULT: AppThemeMode = 'system';
