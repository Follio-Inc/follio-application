/**
 * Resume color theme — thin re-export of the shared document color-theme module.
 */

export {
  resolveDocumentColorTheme,
  resolveResumeColorTheme,
} from '@/lib/document-design/color-theme';
