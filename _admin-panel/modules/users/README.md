# Users module (placeholder)

User monitoring for Follio operators will live here.

**Not implemented yet.** Do not import from `modules/developer`.

When built, expose pages via thin shims under `app/admin/...` and register the module in `_admin-panel/nav.ts`.
